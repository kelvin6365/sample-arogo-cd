apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
  labels:
    run: {{ .Chart.Name }}
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.service.port }}
  - name: https
    port: 443
    protocol: TCP
    targetPort: {{ .Values.service.port }}
  selector:
    run: {{ .Chart.Name }}